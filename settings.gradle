dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        maven { url('https://repo1.maven.org/maven2/') }
    }
}
rootProject.name = "NetExchange"
include ':app'
